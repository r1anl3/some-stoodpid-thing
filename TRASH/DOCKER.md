- RUN AS UBUNTU CONTAINER:
   ```shell
    docker run -i -t ubuntu /bin/bash
   ```

- CREATE DOCKER CONTAINER:
    docker container create
    -i interactively
    -t attached
    exit terminate the /bin/bash, container is not removed.
